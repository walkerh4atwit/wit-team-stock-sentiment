apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: my-app-cert
spec:
  secretName: my-app-tls
  issuerRef:
    name: letsencrypt-cert-issuer
    kind: ClusterIssuer
  dnsNames:
  - dev.sentiments.henrywalker.tech